import React from 'react';
import { Form } from 'redux-form';

const ProfessorProfileForm = ({ handleSubmit }) => (
  <Form onSubmit={handleSubmit}>
  </Form>
);

export default ProfessorProfileForm;
